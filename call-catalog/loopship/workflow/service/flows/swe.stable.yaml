# Fastflow generated release artifact; do not edit directly.
# Edit the matching *.dev.yaml workflow and run the consumer Fastflow promotion flow.
document:
  dsl: 1.0.3
  namespace: service-flows
  name: swe
  version: 0.1.0
  summary: "Loopship swe flow orchestration for Fastflow-native supervision."
  metadata:
    catalog:
      tags:
        - loopship
        - flow
        - swe
    data:
      defaultAdapter: yaml
      adapters:
        yaml:
          rootDir: .
        json:
          rootDir: .
        jsonl:
          rootDir: .
input:
  schema:
    document:
      type: object
      additionalProperties: true
do:
  - read_tasks:
      metadata:
        description: "Read the current Loopship task document through Fastflow workflow data."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      call: fastflow.afn.data.document.read
      with:
        body:
          namespace: .loopship/runtime
          adapter_config:
            rootDir: .
          adapter: yaml
          document: tasks
      output:
        schema:
          document:
            type:
              - object
              - "null"
            additionalProperties: true
        as: "${action.document}"
  - query_events:
      metadata:
        description: "Query the Loopship event log through Fastflow workflow data."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      call: fastflow.afn.data.event-log.query
      with:
        body:
          namespace: .loopship/runtime
          adapter_config:
            rootDir: .
          adapter: jsonl
          log: events
          limit: 25
      output:
        schema:
          document:
            type: array
            items:
              type: object
              additionalProperties: true
        as: "${action.events}"
  - resolve_stage:
      metadata:
        description: "Resolve the active Loopship flow stage from Fastflow workflow-data state."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      set:
        schema_version: loopship.flow-stage/v1
        flow_id: swe
        default_stage: planning
        current_stage: "${[\"planning\", \"awaiting_user_answers\", \"plan_review\", \"task_graph_ready\", \"executing\", \"validating\", \"verification_pending\", \"system_update_pending\", \"landing_ready\", \"replanning\", \"archived\"].includes(String(args.stage || args.state || state.steps.read_tasks?.action?.stage || \"planning\").trim()) ? String(args.stage || args.state || state.steps.read_tasks?.action?.stage || \"planning\").trim() : \"planning\"}"
        runtime:
          tasks: "${state.steps.read_tasks?.action || {}}"
          manifest: null
          events: "${state.steps.query_events?.action || []}"
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.values}"
  - route_stage:
      metadata:
        description: "Route directly to the active Loopship flow stage."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      switch:
        - planning:
            when: "${state.steps.resolve_stage.action.current_stage === \"planning\"}"
            then: stage_planning
        - awaiting_user_answers:
            when: "${state.steps.resolve_stage.action.current_stage === \"awaiting_user_answers\"}"
            then: stage_awaiting_user_answers
        - plan_review:
            when: "${state.steps.resolve_stage.action.current_stage === \"plan_review\"}"
            then: stage_plan_review
        - task_graph_ready_leaf:
            when: "${state.steps.resolve_stage.action.current_stage === \"task_graph_ready\" && (String(state.steps.read_tasks.action.parent_wtree || \"\").trim() || String(state.steps.read_tasks.action.parent_task_id || \"\").trim() || String(state.steps.read_tasks.action.parent_context_ref || \"\").trim() || String(state.steps.read_tasks.action.prompt || \"\").replace(/^loopship:\\s*/i, \"\").trim().toLowerCase().startsWith(\"execute child task \")) && Array.isArray(state.steps.read_tasks.action.tasks) && state.steps.read_tasks.action.tasks.length > 0 && state.steps.read_tasks.action.tasks.every((task) => !String((task || {}).child_wtree || \"\").trim())}"
            then: stage_leaf_git_head
        - task_graph_ready:
            when: "${state.steps.resolve_stage.action.current_stage === \"task_graph_ready\"}"
            then: stage_task_graph_ready
        - executing_leaf:
            when: "${state.steps.resolve_stage.action.current_stage === \"executing\" && Array.isArray(state.steps.read_tasks.action.tasks) && state.steps.read_tasks.action.tasks.length > 0 && state.steps.read_tasks.action.tasks.every((task) => !String((task || {}).child_wtree || \"\").trim())}"
            then: stage_leaf_git_head
        - executing:
            when: "${state.steps.resolve_stage.action.current_stage === \"executing\"}"
            then: stage_executing
        - validating:
            when: "${state.steps.resolve_stage.action.current_stage === \"validating\"}"
            then: stage_validating
        - verification_pending:
            when: "${state.steps.resolve_stage.action.current_stage === \"verification_pending\"}"
            then: stage_verification_pending
        - system_update_pending:
            when: "${state.steps.resolve_stage.action.current_stage === \"system_update_pending\"}"
            then: stage_system_update_pending
        - landing_ready:
            when: "${state.steps.resolve_stage.action.current_stage === \"landing_ready\"}"
            then: stage_landing_ready
        - replanning:
            when: "${state.steps.resolve_stage.action.current_stage === \"replanning\"}"
            then: stage_replanning
        - archived:
            when: "${state.steps.resolve_stage.action.current_stage === \"archived\"}"
            then: stage_archived
  - stage_planning:
      then: stage_result_planning
      metadata:
        description: "Run Loopship planning via the pinned plan step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:e6d5daed6bac0cbc8ef3f41ca52ec2a5d5636e5c335dff03bbb6c7980e32c5d2"
      call: loopship.workflow.service.step.plan
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: planning
          summary: "Submit a decision-complete plan payload with explicit system context. Ask or default every high-impact unknown."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${state.steps.read_tasks.action.coordinator_worktree || inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          parent_task_id: "${state.steps.read_tasks.action.parent_task_id || ''}"
          parent_context_ref: "${state.steps.read_tasks.action.parent_context_ref || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          replan_reason: "${state.steps.read_tasks.action.replan_reason || ''}"
          requirements:
            - "Classify the request."
            - "Use repository discovery before asking questions."
            - "Use AF to surface contradictions, hidden assumptions, weak evidence, and edge cases."
            - "Use OF to collapse scope, defaults, acceptance, decomposition, and verification targets."
            - "For greenfield app/product work, ask or explicitly default every high-impact unknown before task graph approval."
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_awaiting_user_answers:
      then: stage_result_awaiting_user_answers
      metadata:
        description: "Run Loopship awaiting_user_answers via the pinned questions step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:c148837eda2d41990ae817e4a4d2a777105dc0edb904ab6d103acf31b5af482f"
      call: loopship.workflow.service.step.questions
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: awaiting_user_answers
          summary: "Submit grouped user answers for the current clarification round."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${state.steps.read_tasks.action.coordinator_worktree || inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_plan_review:
      then: stage_result_plan_review
      metadata:
        description: "Run Loopship plan_review via the pinned task_graph step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:0b91052928aaa130f1e02507b674f5df83e2631f67550ec7cc9624e04b111d33"
      call: loopship.workflow.service.step.task-graph
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: plan_review
          summary: "Approve or reject the generated task graph, including a replan reason when rejected."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_task_graph_ready:
      then: stage_result_task_graph_ready
      metadata:
        description: "Run Loopship task_graph_ready via the pinned executing step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:f24f85241d816f088111e6fcf5f5618572d313cb1b2e986381ae8788d07a5d56"
      call: loopship.workflow.service.step.executing
      with:
        version: 0.1.0
        input:
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          quest: "${state.steps.read_tasks.action || {}}"
          children: "${inputs.children || state.steps.read_tasks.action.tasks || []}"
          actor: "${inputs.actor || 'loopship'}"
          target_branch: "${inputs.target_branch || state.steps.read_tasks.action.landing_target_branch || 'main'}"
          target_worktree: "${inputs.target_worktree || state.steps.read_tasks.action.landing_target_worktree || ''}"
          source_branch: "${inputs.source_branch || state.steps.read_tasks.action.coordinator_branch || ''}"
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_executing:
      then: stage_result_executing
      metadata:
        description: "Run Loopship executing via the pinned child_result step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:2328d11fb655526c7aff8a9c866be93c100b100878671204ee42eb397293bb8a"
      call: loopship.workflow.service.step.child-result
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: executing
          summary: "Submit the result of a completed child quest back to the parent flow."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_validating:
      then: stage_result_validating
      metadata:
        description: "Run Loopship validating via the pinned validation step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:4d75790590dac127845f936eb88524c2e69db8c3d35184cbf2520265244d36c1"
      call: loopship.workflow.service.step.validation
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: validating
          summary: "Submit deterministic validation results only, including schemas, DAG, tests, build, lint/typecheck, manifest, and behavior refs."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_verification_pending:
      then: stage_result_verification_pending
      metadata:
        description: "Run Loopship verification_pending via the pinned verification step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:0a3db8a8e14b6cf321102b7d97af43293101fe37fd592f912a00818d69626fb9"
      call: loopship.workflow.service.step.verification
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: verification_pending
          summary: "Submit acceptance trace, PR-style risk review, and residual risks."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_system_update_pending:
      then: stage_result_system_update_pending
      metadata:
        description: "Run Loopship system_update_pending via the pinned system_update step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:5ec976217ce409e5ec85256b39d336d7cf5bd6f13b19029172c84e0e5a0086e9"
      call: loopship.workflow.service.step.system-update
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          repo: "${state.steps.read_tasks.action.coordinator_worktree || inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: "${state.steps.read_tasks.action.flow_id || 'swe'}"
          flow_version: "${state.steps.read_tasks.action.flow_version || 1}"
          current_stage: system_update_pending
          summary: "Submit system doc updates. Loopship writes signed repo docs."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.prompt || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${state.steps.read_tasks.action.tasks || []}"
          question_rounds: "${state.steps.read_tasks.action.question_rounds || []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || { status: '', checks: [] }}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || { status: '', acceptance_trace: [], risks: [] }}"
          events: "${state.steps.query_events.action || []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          parent_task_id: "${state.steps.read_tasks.action.parent_task_id || ''}"
          parent_context_ref: "${state.steps.read_tasks.action.parent_context_ref || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          actor: "${inputs.actor || 'loopship'}"
          landing_target_branch: "${inputs.target_branch || state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${inputs.target_worktree || state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_landing_ready:
      then: stage_result_landing_ready
      metadata:
        description: "Run Loopship landing_ready via the pinned landing step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:a67b2cd9c43c3ed05f654f6429fdf34f7a8a699a20073db9186f2d945cb91bc4"
      call: loopship.workflow.service.step.landing
      with:
        version: 0.1.0
        input:
          status: "${inputs.status || 'landed'}"
          summary: "${inputs.summary || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          runtime: "${inputs.runtime || ''}"
          actor: "${inputs.actor || 'loopship'}"
          target_branch: "${inputs.target_branch || state.steps.read_tasks.action.landing_target_branch || 'main'}"
          target_worktree: "${inputs.target_worktree || state.steps.read_tasks.action.landing_target_worktree || ''}"
          source_branch: "${inputs.source_branch || state.steps.read_tasks.action.coordinator_branch || ''}"
          next_stage: "${(inputs.status || 'landed') === 'blocked' ? 'landing_ready' : 'archived'}"
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_replanning:
      then: stage_result_replanning
      metadata:
        description: "Run Loopship replanning via the pinned plan step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:e6d5daed6bac0cbc8ef3f41ca52ec2a5d5636e5c335dff03bbb6c7980e32c5d2"
      call: loopship.workflow.service.step.plan
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: replanning
          summary: "Submit a decision-complete plan payload with explicit system context. Ask or default every high-impact unknown."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          parent_task_id: "${state.steps.read_tasks.action.parent_task_id || ''}"
          parent_context_ref: "${state.steps.read_tasks.action.parent_context_ref || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          replan_reason: "${state.steps.read_tasks.action.replan_reason || ''}"
          requirements:
            - "Classify the request."
            - "Use repository discovery before asking questions."
            - "Use AF to surface contradictions, hidden assumptions, weak evidence, and edge cases."
            - "Use OF to collapse scope, defaults, acceptance, decomposition, and verification targets."
            - "For greenfield app/product work, ask or explicitly default every high-impact unknown before task graph approval."
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_archived:
      then: stage_result_archived
      metadata:
        description: "Run Loopship archived via the pinned archived step workflow."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
        ref:
          digest: "sha256:07069497de8d832b6169af3656ea6e02699d0f0581726224bb5c2236437df9a7"
      call: loopship.workflow.service.step.archived
      with:
        version: 0.1.0
        input:
          schema_version: loopship.step.request/v1
          kind: quest_step_request
          wtree: "${state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          quest_id: "${state.steps.read_tasks.action.quest_id || state.steps.read_tasks.action.wtree || inputs.wtree || ''}"
          flow_id: swe
          flow_version: 1
          current_stage: archived
          summary: "Report final quest state only."
          prompt: "${state.steps.read_tasks.action.prompt || inputs.request || ''}"
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          repoRoot: "${inputs.repoRoot || inputs.repo || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          runtime: "${inputs.runtime || ''}"
          tasks: "${Array.isArray(state.steps.read_tasks.action.tasks) ? state.steps.read_tasks.action.tasks : []}"
          question_rounds: "${Array.isArray(state.steps.read_tasks.action.question_rounds) ? state.steps.read_tasks.action.question_rounds : []}"
          plan_detail: "${state.steps.read_tasks.action.plan_detail || {}}"
          validation_receipt: "${state.steps.read_tasks.action.validation_receipt || {}}"
          verification_receipt: "${state.steps.read_tasks.action.verification_receipt || {}}"
          events: "${Array.isArray(state.steps.query_events.action) ? state.steps.query_events.action : []}"
          parent_wtree: "${state.steps.read_tasks.action.parent_wtree || ''}"
          coordinator_branch: "${state.steps.read_tasks.action.coordinator_branch || ''}"
          coordinator_worktree: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          landing_target_branch: "${state.steps.read_tasks.action.landing_target_branch || 'main'}"
          landing_target_worktree: "${state.steps.read_tasks.action.landing_target_worktree || ''}"
          requirements: []
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_result_planning:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function stringList(value) { return array(value).map((item) => String(item || "").trim()).filter(Boolean); }
            function normalizeName(value) { return String(value || "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").replace(/-+/g, "-") || "main"; }
            function normalizeTaskPathSegment(value) { return String(value || "").trim().replace(/[\\/]+/g, "-").replace(/\s+/g, "-").replace(/[^A-Za-z0-9._-]+/g, "-").replace(/-+/g, "-").replace(/^-+|-+$/g, "") || "task"; }
            function compactDigest(value) { const text = String(value || ""); const fnv = (seed) => { let hash = seed; for (let index = 0; index < text.length; index += 1) { hash ^= text.charCodeAt(index); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, "0"); }; return (fnv(2166136261) + fnv(2166136261 ^ 0x9e3779b9)).slice(0, 12); }
            function assignmentKey(wtree, taskId) { const left = normalizeTaskPathSegment(wtree || "quest"); const right = normalizeTaskPathSegment(taskId || "task"); const full = [left, right].filter(Boolean).join("-"); if (full.length <= 72) return full; const digest = compactDigest(full); const taskPart = right.slice(0, 20).replace(/-+$/g, "") || "task"; const wtreeBudget = Math.max(16, 72 - taskPart.length - digest.length - 2); const wtreePart = left.slice(0, wtreeBudget).replace(/-+$/g, "") || "quest"; return [wtreePart, taskPart, digest].join("-"); }
            function taskAcceptance(value) { return Array.isArray(value) ? stringList(value).join("; ") : String(value || "").trim(); }
            function normalizeSystemContext(value) { const row = object(value); return { relevant_object_refs: stringList(row.relevant_object_refs), relevant_assertion_refs: stringList(row.relevant_assertion_refs), relevant_resource_refs: stringList(row.relevant_resource_refs), relevant_memory_refs: stringList(row.relevant_memory_refs), durable_implications: array(row.durable_implications).filter((item) => Object.keys(object(item)).length) }; }
            function normalizePrompt(value) { return String(value || "").toLowerCase().replace(/^loopship:\s*/, "").replace(/[^a-z0-9]+/g, " ").trim(); }
            function isTerminalChild(tasks) { return Boolean(String(tasks.parent_wtree || "").trim()) || Boolean(String(tasks.parent_task_id || "").trim()) || Boolean(String(tasks.parent_context_ref || "").trim()) || normalizePrompt(tasks.prompt).startsWith("execute child task "); }
            function normalizePlanTask(tasks, input, index) { const task = object(input); const wtree = String(tasks.wtree || "quest"); const id = normalizeName(task.id || task.task_id || "task-" + (index + 1)); const contextRoot = String(tasks.context_root || "."); const terminalChild = isTerminalChild(tasks); const coordinatorBranch = String(tasks.coordinator_branch || "main"); const coordinatorWorktree = String(tasks.coordinator_worktree || contextRoot); const landingTargetBranch = String(tasks.landing_target_branch || coordinatorBranch); return { id, title: String(task.title || task.name || id), type: task.type === "general" ? "general" : "coding", status: String(task.status || (terminalChild ? "pending" : "child_received")), dependencies: stringList(task.dependencies || task.depends_on).map(normalizeName), scope_files: stringList(task.scope_files || task.scope), spec_refs: stringList(task.spec_refs || task.specs), context_refs: stringList(task.context_refs || task.context), branch_ref: terminalChild ? coordinatorBranch : String(task.branch_ref || "codex/" + assignmentKey(wtree, id)), worktree_path: terminalChild ? coordinatorWorktree : String(task.worktree_path || contextRoot.replace(/[\\/]+$/g, "") + "/worktrees/" + assignmentKey(wtree, id)), child_wtree: terminalChild ? "" : String(task.child_wtree || assignmentKey(wtree, id)), concurrency_group: String(task.concurrency_group || ""), merge_target: String(task.merge_target || (terminalChild ? landingTargetBranch : coordinatorBranch)), merge_lease_id: terminalChild ? "" : String(task.merge_lease_id || "lease-" + assignmentKey(wtree, id)), merge_commit: String(task.merge_commit || ""), system_impact_ref: String(task.system_impact_ref || ""), acceptance: taskAcceptance(task.acceptance || task.acceptance_criteria) }; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function matchesPlan(value) { const row = object(value); return "classification" in row || "questions" in row || "task_graph" in row; }
            function findPlanPayload(value, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPlanPayload(item, depth + 1); if (matchesPlan(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (matchesPlan(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPlanPayload(row[key], depth + 1); if (matchesPlan(found)) return found; } } for (const item of Object.values(row)) { const found = findPlanPayload(item, depth + 1); if (matchesPlan(found)) return found; } return {}; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_planning?.action);
            const payload = findPlanPayload(stageAction);
            const questions = array(payload.questions);
            const targetStage = questions.length ? "awaiting_user_answers" : "plan_review";
            const transition = questions.length ? "questions" : "planned";
            const patch = questions.length ? { stage: targetStage, replan_reason: "", question_rounds: [...array(tasks.question_rounds), { questions }] } : { stage: targetStage, replan_reason: "", assumptions: stringList(payload.assumptions), constraints: stringList(payload.constraints), plan_detail: { classification: String(payload.classification || ""), scope: String(payload.scope || ""), summary: String(payload.summary || payload.scope || ""), rationale: String(payload.summary || payload.scope || ""), system_context: normalizeSystemContext(payload.system_context), high_impact_unknowns: stringList(payload.high_impact_unknowns), defaulted_unknowns: stringList(payload.defaulted_unknowns), verification_targets: stringList(payload.verification_targets), decomposition_rationale: String(payload.decomposition_rationale || payload.summary || "") }, tasks: array(object(payload.task_graph).tasks).map((task, index) => normalizePlanTask(tasks, task, index)) };
            const event = questions.length ? { event: "question_round", stage: targetStage, question_count: questions.length } : { event: "plan_submitted", stage: targetStage, classification: String(payload.classification || ""), task_count: array(object(payload.task_graph).tasks).length, verification_target_count: array(payload.verification_targets).length };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "planning", stage_after: targetStage, transition, step: "plan", step_workflow_task: "stage_planning", step_payload: payload, step_action: payload, state_patch: patch, events: eventList("planning", targetStage, transition, "plan", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "plan", stage_before: "planning", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_replanning:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function stringList(value) { return array(value).map((item) => String(item || "").trim()).filter(Boolean); }
            function normalizeName(value) { return String(value || "").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "").replace(/-+/g, "-") || "main"; }
            function normalizeTaskPathSegment(value) { return String(value || "").trim().replace(/[\\/]+/g, "-").replace(/\s+/g, "-").replace(/[^A-Za-z0-9._-]+/g, "-").replace(/-+/g, "-").replace(/^-+|-+$/g, "") || "task"; }
            function compactDigest(value) { const text = String(value || ""); const fnv = (seed) => { let hash = seed; for (let index = 0; index < text.length; index += 1) { hash ^= text.charCodeAt(index); hash = Math.imul(hash, 16777619); } return (hash >>> 0).toString(16).padStart(8, "0"); }; return (fnv(2166136261) + fnv(2166136261 ^ 0x9e3779b9)).slice(0, 12); }
            function assignmentKey(wtree, taskId) { const left = normalizeTaskPathSegment(wtree || "quest"); const right = normalizeTaskPathSegment(taskId || "task"); const full = [left, right].filter(Boolean).join("-"); if (full.length <= 72) return full; const digest = compactDigest(full); const taskPart = right.slice(0, 20).replace(/-+$/g, "") || "task"; const wtreeBudget = Math.max(16, 72 - taskPart.length - digest.length - 2); const wtreePart = left.slice(0, wtreeBudget).replace(/-+$/g, "") || "quest"; return [wtreePart, taskPart, digest].join("-"); }
            function taskAcceptance(value) { return Array.isArray(value) ? stringList(value).join("; ") : String(value || "").trim(); }
            function normalizeSystemContext(value) { const row = object(value); return { relevant_object_refs: stringList(row.relevant_object_refs), relevant_assertion_refs: stringList(row.relevant_assertion_refs), relevant_resource_refs: stringList(row.relevant_resource_refs), relevant_memory_refs: stringList(row.relevant_memory_refs), durable_implications: array(row.durable_implications).filter((item) => Object.keys(object(item)).length) }; }
            function normalizePrompt(value) { return String(value || "").toLowerCase().replace(/^loopship:\s*/, "").replace(/[^a-z0-9]+/g, " ").trim(); }
            function isTerminalChild(tasks) { return Boolean(String(tasks.parent_wtree || "").trim()) || Boolean(String(tasks.parent_task_id || "").trim()) || Boolean(String(tasks.parent_context_ref || "").trim()) || normalizePrompt(tasks.prompt).startsWith("execute child task "); }
            function normalizePlanTask(tasks, input, index) { const task = object(input); const wtree = String(tasks.wtree || "quest"); const id = normalizeName(task.id || task.task_id || "task-" + (index + 1)); const contextRoot = String(tasks.context_root || "."); const terminalChild = isTerminalChild(tasks); const coordinatorBranch = String(tasks.coordinator_branch || "main"); const coordinatorWorktree = String(tasks.coordinator_worktree || contextRoot); const landingTargetBranch = String(tasks.landing_target_branch || coordinatorBranch); return { id, title: String(task.title || task.name || id), type: task.type === "general" ? "general" : "coding", status: String(task.status || (terminalChild ? "pending" : "child_received")), dependencies: stringList(task.dependencies || task.depends_on).map(normalizeName), scope_files: stringList(task.scope_files || task.scope), spec_refs: stringList(task.spec_refs || task.specs), context_refs: stringList(task.context_refs || task.context), branch_ref: terminalChild ? coordinatorBranch : String(task.branch_ref || "codex/" + assignmentKey(wtree, id)), worktree_path: terminalChild ? coordinatorWorktree : String(task.worktree_path || contextRoot.replace(/[\\/]+$/g, "") + "/worktrees/" + assignmentKey(wtree, id)), child_wtree: terminalChild ? "" : String(task.child_wtree || assignmentKey(wtree, id)), concurrency_group: String(task.concurrency_group || ""), merge_target: String(task.merge_target || (terminalChild ? landingTargetBranch : coordinatorBranch)), merge_lease_id: terminalChild ? "" : String(task.merge_lease_id || "lease-" + assignmentKey(wtree, id)), merge_commit: String(task.merge_commit || ""), system_impact_ref: String(task.system_impact_ref || ""), acceptance: taskAcceptance(task.acceptance || task.acceptance_criteria) }; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function matchesPlan(value) { const row = object(value); return "classification" in row || "questions" in row || "task_graph" in row; }
            function findPlanPayload(value, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPlanPayload(item, depth + 1); if (matchesPlan(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (matchesPlan(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPlanPayload(row[key], depth + 1); if (matchesPlan(found)) return found; } } for (const item of Object.values(row)) { const found = findPlanPayload(item, depth + 1); if (matchesPlan(found)) return found; } return {}; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_replanning?.action);
            const payload = findPlanPayload(stageAction);
            const questions = array(payload.questions);
            const targetStage = questions.length ? "awaiting_user_answers" : "plan_review";
            const transition = questions.length ? "questions" : "planned";
            const patch = questions.length ? { stage: targetStage, replan_reason: "", question_rounds: [...array(tasks.question_rounds), { questions }] } : { stage: targetStage, replan_reason: "", assumptions: stringList(payload.assumptions), constraints: stringList(payload.constraints), plan_detail: { classification: String(payload.classification || ""), scope: String(payload.scope || ""), summary: String(payload.summary || payload.scope || ""), rationale: String(payload.summary || payload.scope || ""), system_context: normalizeSystemContext(payload.system_context), high_impact_unknowns: stringList(payload.high_impact_unknowns), defaulted_unknowns: stringList(payload.defaulted_unknowns), verification_targets: stringList(payload.verification_targets), decomposition_rationale: String(payload.decomposition_rationale || payload.summary || "") }, tasks: array(object(payload.task_graph).tasks).map((task, index) => normalizePlanTask(tasks, task, index)) };
            const event = questions.length ? { event: "question_round", stage: targetStage, question_count: questions.length } : { event: "plan_submitted", stage: targetStage, classification: String(payload.classification || ""), task_count: array(object(payload.task_graph).tasks).length, verification_target_count: array(payload.verification_targets).length };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "replanning", stage_after: targetStage, transition, step: "plan", step_workflow_task: "stage_replanning", step_payload: payload, step_action: payload, state_patch: patch, events: eventList("replanning", targetStage, transition, "plan", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "plan", stage_before: "replanning", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_awaiting_user_answers:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function findPayload(value, match, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (match(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPayload(row[key], match, depth + 1); if (match(found)) return found; } } for (const item of Object.values(row)) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; }
            function answerKey(answer) { return String(answer.question_id || answer.id || "").trim(); }
            function mergeAnswersIntoLatestQuestionRound(tasks, answers) { const rounds = array(tasks.question_rounds).map((round) => ({ ...object(round), questions: array(object(round).questions) })); if (!rounds.length) return rounds; const latest = rounds[rounds.length - 1]; const byId = new Map(); const byQuestion = new Map(); for (const answer of answers) { const row = object(answer); const key = answerKey(row); if (key) byId.set(key, row); const question = String(row.question || "").trim(); if (question) byQuestion.set(question, row); } latest.questions = array(latest.questions).map((question) => { const row = object(question); const answer = byId.get(String(row.id || "").trim()) || byQuestion.get(String(row.question || "").trim()); if (!answer) return row; return { ...row, status: answer.accepted_default ? "defaulted" : "answered", answer: String(answer.answer || "").trim(), accepted_default: Boolean(answer.accepted_default) }; }); return rounds; }
            function latestRoundComplete(rounds) { const latest = array(rounds)[array(rounds).length - 1]; const questions = array(object(latest).questions); return questions.length > 0 && questions.every((question) => { const row = object(question); const status = String(row.status || "").trim(); if (status === "answered" || status === "defaulted") return true; return Boolean(String(row.answer || "").trim()); }); }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_awaiting_user_answers?.action);
            const payload = findPayload(stageAction, (row) => "answers" in object(row));
            const questionRounds = mergeAnswersIntoLatestQuestionRound(tasks, array(payload.answers));
            const complete = latestRoundComplete(questionRounds);
            const targetStage = complete ? "planning" : "awaiting_user_answers";
            const transition = complete ? "answered" : "partial";
            const patch = { stage: targetStage, question_rounds: questionRounds };
            const event = { event: complete ? "answers_submitted" : "answers_partial", stage: targetStage, answer_count: array(payload.answers).length };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "awaiting_user_answers", stage_after: targetStage, transition, step: "questions", step_workflow_task: "stage_awaiting_user_answers", step_payload: payload, step_action: payload, state_patch: patch, events: eventList("awaiting_user_answers", targetStage, transition, "questions", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "questions", stage_before: "awaiting_user_answers", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_plan_review:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function normalizePrompt(value) { return String(value || "").toLowerCase().replace(/^loopship:\s*/, "").replace(/[^a-z0-9]+/g, " ").trim(); }
            function isTerminalChild(tasks) { return Boolean(String(tasks.parent_wtree || "").trim()) || Boolean(String(tasks.parent_task_id || "").trim()) || Boolean(String(tasks.parent_context_ref || "").trim()) || normalizePrompt(tasks.prompt).startsWith("execute child task "); }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            function matchesTaskGraphReview(value) { const row = object(value); return typeof row.approved === "boolean" || typeof row.rejected === "boolean"; }
            function findTaskGraphReviewPayload(value, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findTaskGraphReviewPayload(item, depth + 1); if (matchesTaskGraphReview(found)) return found; } return {}; } const row = object(value); if (matchesTaskGraphReview(row)) return row; for (const key of ["decision", "output", "result", "workflow", "action", "value", "answer", "response"]) { if (key in row) { const found = findTaskGraphReviewPayload(row[key], depth + 1); if (matchesTaskGraphReview(found)) return found; } } for (const item of Object.values(row)) { const found = findTaskGraphReviewPayload(item, depth + 1); if (matchesTaskGraphReview(found)) return found; } return {}; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_plan_review?.action);
            const payload = findTaskGraphReviewPayload(stageAction);
            const rejected = payload.approved === false || payload.rejected === true;
            const emptyApproved = !rejected && payload.approved === true && array(tasks.tasks).length === 0;
            const effectiveRejected = rejected || emptyApproved;
            const replanReason = rejected ? String(payload.replan_reason || "") : emptyApproved ? "approved task graph is empty; replan with at least one concrete local or child task before execution" : "";
            const targetStage = effectiveRejected ? "replanning" : isTerminalChild(tasks) ? "executing" : "task_graph_ready";
            const transition = effectiveRejected ? "rejected" : "approved";
            const event = { event: effectiveRejected ? "task_graph_rejected" : "task_graph_approved", stage: targetStage, replan_reason: replanReason };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "plan_review", stage_after: targetStage, transition, step: "task_graph", step_workflow_task: "stage_plan_review", step_payload: payload, step_action: payload, state_patch: effectiveRejected ? { stage: targetStage, replan_reason: replanReason } : { stage: targetStage, replan_reason: "" }, events: eventList("plan_review", targetStage, transition, "task_graph", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "task_graph", stage_before: "plan_review", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_task_graph_ready:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function findPayload(value, match, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (match(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPayload(row[key], match, depth + 1); if (match(found)) return found; } } for (const item of Object.values(row)) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; }
            function taskId(value) { const row = object(value); return String(row.task_id || row.id || "").trim(); }
            function preparedChildren(value) { const row = object(value); const listed = array(row.prepared_children).concat(array(row.children)).map((item) => object(item)).filter((item) => taskId(item)); if (listed.length) return listed; return taskId(row) ? [row] : []; }
            function tasksAfterChildPrepare(tasks, value) { const prepared = preparedChildren(value); if (!prepared.length) return tasks; const byId = new Map(prepared.map((item) => [taskId(item), item])); return { ...tasks, tasks: array(tasks.tasks).map((task) => { const row = object(task); const launch = byId.get(taskId(row)); if (!launch) return task; return { ...row, status: "child_dispatched", child_wtree: String(launch.child_wtree || row.child_wtree || ""), branch_ref: String(launch.branch_ref || row.branch_ref || ""), worktree_path: String(launch.worktree_path || row.worktree_path || ""), merge_target: String(launch.merge_target || row.merge_target || ""), merge_lease_id: String(launch.merge_lease_id || row.merge_lease_id || "") }; }) }; }
            function terminalTaskState(tasks) { const statuses = array(object(tasks).tasks).map((task) => String(object(task).status || "child_received")).filter(Boolean); if (!statuses.length) return "unknown"; const done = new Set(["child_merged", "child_archived", "done", "merged"]); if (statuses.every((status) => done.has(status))) return "all_done"; return "continue"; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_task_graph_ready?.action);
            const payload = findPayload(stageAction, (row) => array(object(row).prepared_children).length > 0 || array(object(row).children).length > 0 || "task_id" in object(row) || "child_wtree" in object(row));
            const nextTasks = tasksAfterChildPrepare(tasks, payload);
            const transition = terminalTaskState(nextTasks) === "all_done" ? "complete" : "partial";
            const targetStage = transition === "complete" ? "validating" : "executing";
            const event = { event: "step_submitted", stage: targetStage, step: "executing" };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "task_graph_ready", stage_after: targetStage, transition, step: "executing", step_workflow_task: "stage_task_graph_ready", step_payload: payload, step_action: payload, state_patch: { stage: targetStage, tasks: array(nextTasks.tasks) }, events: eventList("task_graph_ready", targetStage, transition, "executing", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "executing", stage_before: "task_graph_ready", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_leaf_git_head:
      then: stage_leaf_target_git_head
      metadata:
        description: "Resolve the leaf child worktree HEAD commit for lifecycle evidence."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      call: loopship.afn.service.git.head
      with:
        body:
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          cwd: "${state.steps.read_tasks.action.coordinator_worktree || ''}"
          ref: HEAD
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_leaf_target_git_head:
      then: stage_result_leaf_executing
      metadata:
        description: "Resolve the landing target HEAD so leaf execution cannot complete without a new local commit."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      call: loopship.afn.service.git.head
      with:
        body:
          repo: "${inputs.repo || inputs.repoRoot || state.steps.read_tasks.action.context_root || env.PWD || ''}"
          cwd: "${state.steps.read_tasks.action.context_root || inputs.repo || inputs.repoRoot || env.PWD || ''}"
          ref: "${state.steps.read_tasks.action.landing_target_branch || state.steps.read_tasks.action.parent_wtree || 'main'}"
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - stage_result_leaf_executing:
      then: persist_stage
      metadata:
        description: "Record leaf child execution without requiring a nested child_result callback."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function isDoneStatus(status) { return new Set(["child_merged", "child_archived", "done", "merged"]).has(String(status || "")); }
            function markLeafTasksDone(tasks, mergeCommit) { return { ...tasks, tasks: array(tasks.tasks).map((task) => { const row = object(task); if (String(row.child_wtree || "").trim()) return row; const status = String(row.status || "pending"); const nextStatus = status === "blocked" || status === "failed" ? status : "done"; return { ...row, status: nextStatus, merge_commit: String(mergeCommit || row.merge_commit || "") }; }) }; }
            function localTaskIds(tasks) { return array(object(tasks).tasks).map((task) => object(task)).filter((task) => !String(task.child_wtree || "").trim()).map((task, index) => String(task.id || task.task_id || "task-" + (index + 1)).trim()).filter(Boolean); }
            function pendingLocalTaskIds(tasks) { return array(object(tasks).tasks).map((task) => object(task)).filter((task) => !String(task.child_wtree || "").trim() && !isDoneStatus(task.status)).map((task, index) => String(task.id || task.task_id || "task-" + (index + 1)).trim()).filter(Boolean); }
            function localWorkReceipt(tasks, mergeCommit, targetCommit, status) { const missing = status === "missing_local_commit"; return { mode: "shared-head-commit", worktree_path: String(tasks.coordinator_worktree || ""), commit: mergeCommit, target_commit: targetCommit, status, covered_task_ids: missing ? [] : localTaskIds(tasks), pending_task_ids: pendingLocalTaskIds(tasks) }; }
            function terminalTaskState(tasks) { const statuses = array(object(tasks).tasks).map((task) => String(object(task).status || "child_received")).filter(Boolean); if (!statuses.length) return "unknown"; const done = new Set(["child_merged", "child_archived", "done", "merged"]); if (statuses.every((status) => done.has(status))) return "all_done"; return "continue"; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const mergeCommit = String(object(state.steps.stage_leaf_git_head?.action).commit || "");
            const targetCommit = String(object(state.steps.stage_leaf_target_git_head?.action).commit || "");
            const pendingTaskIds = pendingLocalTaskIds(tasks);
            if (pendingTaskIds.length && (!mergeCommit || (targetCommit && mergeCommit === targetCommit))) {
              const receipt = localWorkReceipt(tasks, mergeCommit, targetCommit, "missing_local_commit");
              const event = { event: "leaf_execution_missing_commit", stage: "executing", task_count: array(tasks.tasks).length, merge_commit: mergeCommit, target_commit: targetCommit, pending_task_ids: pendingTaskIds };
              return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "executing", stage_after: "executing", transition: "blocked", step: "executing", step_workflow_task: "stage_result_leaf_executing", step_payload: { task_count: array(tasks.tasks).length, merge_commit: mergeCommit, target_commit: targetCommit, local_work_receipt: receipt }, step_action: { task_count: array(tasks.tasks).length, merge_commit: mergeCommit, target_commit: targetCommit, local_work_receipt: receipt }, state_patch: { stage: "executing", tasks: array(tasks.tasks), local_work_receipt: receipt }, events: eventList("executing", "executing", "blocked", "executing", event), event_payload: { event: "stage_changed", stage: "executing", transition: "blocked", step: "executing", stage_before: "executing", stage_after: "executing" }, runtime: runtimeSnapshot() };
            }
            const nextTasks = markLeafTasksDone(tasks, mergeCommit);
            const receipt = localWorkReceipt(nextTasks, mergeCommit, targetCommit, "recorded");
            const transition = terminalTaskState(nextTasks) === "all_done" ? "complete" : "partial";
            const targetStage = transition === "complete" ? "validating" : "executing";
            const event = { event: "leaf_execution_recorded", stage: targetStage, task_count: array(nextTasks.tasks).length, merge_commit: mergeCommit, target_commit: targetCommit, covered_task_ids: receipt.covered_task_ids };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "executing", stage_after: targetStage, transition, step: "executing", step_workflow_task: "stage_result_leaf_executing", step_payload: { task_count: array(nextTasks.tasks).length, merge_commit: mergeCommit, target_commit: targetCommit, local_work_receipt: receipt }, step_action: { task_count: array(nextTasks.tasks).length, merge_commit: mergeCommit, target_commit: targetCommit, local_work_receipt: receipt }, state_patch: { stage: targetStage, tasks: array(nextTasks.tasks), local_work_receipt: receipt }, events: eventList("executing", targetStage, transition, "executing", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "executing", stage_before: "executing", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_executing:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function findPayload(value, match, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (match(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPayload(row[key], match, depth + 1); if (match(found)) return found; } } for (const item of Object.values(row)) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; }
            function taskId(value) { const row = object(value); return String(row.task_id || row.id || "").trim(); }
            function taskDependencies(value) { const row = object(value); const raw = Array.isArray(row.dependencies) ? row.dependencies : Array.isArray(row.depends_on) ? row.depends_on : []; return raw.map((entry) => String(entry || "").trim()).filter(Boolean); }
            function tasksAfterChildResult(tasks, payload) { const taskId = String(payload.task_id || payload.id || ""); const items = array(tasks.tasks); if (!taskId || !items.length) return tasks; return { ...tasks, tasks: items.map((task) => { const row = object(task); if (String(row.id || "") !== taskId) return task; const status = String(payload.status || ""); return { ...row, status: status === "passed" ? "child_archived" : status === "blocked" ? "blocked" : "failed", child_wtree: String(payload.child_wtree || row.child_wtree || ""), branch_ref: String(payload.branch_ref || row.branch_ref || ""), worktree_path: String(payload.worktree_path || row.worktree_path || ""), merge_target: String(payload.merge_target || row.merge_target || ""), merge_lease_id: String(payload.merge_lease_id || row.merge_lease_id || ""), merge_commit: String(payload.merge_commit || row.merge_commit || "") }; }) }; }
            function childResultsAfterChildResult(tasks, payload) { const taskId = String(payload.task_id || payload.id || "").trim(); const childWtree = String(payload.child_wtree || "").trim(); if (!taskId || !childWtree) return array(tasks.child_results); const next = { task_id: taskId, child_wtree: childWtree, status: String(payload.status || "").trim(), evidence: array(payload.evidence).map((entry) => object(entry)).filter((entry) => String(entry.type || "").trim() && String(entry.ref || "").trim()), branch_ref: String(payload.branch_ref || "").trim(), worktree_path: String(payload.worktree_path || "").trim(), merge_target: String(payload.merge_target || "").trim(), merge_lease_id: String(payload.merge_lease_id || "").trim(), merge_commit: String(payload.merge_commit || "").trim() }; const existing = array(tasks.child_results).filter((entry) => { const row = object(entry); return !(String(row.task_id || "").trim() === taskId && String(row.child_wtree || "").trim() === childWtree); }); return [...existing, next]; }
            function terminalTaskState(tasks) { const statuses = array(object(tasks).tasks).map((task) => String(object(task).status || "child_received")).filter(Boolean); if (!statuses.length) return "unknown"; const done = new Set(["child_merged", "child_archived", "done", "merged"]); if (statuses.every((status) => done.has(status))) return "all_done"; return "continue"; }
            function hasReadyUndispatchedChildren(tasks) { const items = array(object(tasks).tasks).map((task) => object(task)); const statusById = new Map(items.map((task) => [taskId(task), String(task.status || "child_received")])); const done = new Set(["child_merged", "child_archived", "done", "merged"]); return items.some((task) => { const status = String(task.status || "child_received"); if (!(status === "child_received" || status === "pending" || status === "queued")) return false; return taskDependencies(task).every((dependencyId) => { const dependencyStatus = statusById.get(dependencyId); return typeof dependencyStatus === "string" && done.has(dependencyStatus); }); }); }
            function hasFailedChildren(tasks) { return array(object(tasks).tasks).some((task) => { const status = String(object(task).status || "child_received"); return status === "blocked" || status === "failed"; }); }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_executing?.action);
            const payload = findPayload(stageAction, (row) => "child_wtree" in object(row) || "merge_commit" in object(row) || "task_id" in object(row));
            const nextTasks = tasksAfterChildResult(tasks, payload);
            const complete = terminalTaskState(nextTasks) === "all_done";
            const queueNextChild = !complete && !hasFailedChildren(nextTasks) && hasReadyUndispatchedChildren(nextTasks);
            const transition = complete ? "complete" : queueNextChild ? nextTasks.supervise_step === true ? "next_child" : "next_children" : "partial";
            const targetStage = complete ? "validating" : queueNextChild ? "task_graph_ready" : "executing";
            const patch = { stage: targetStage, tasks: array(nextTasks.tasks), child_results: childResultsAfterChildResult(tasks, payload) };
            const event = { event: "child_result_submitted", stage: targetStage, task_id: String(payload.task_id || payload.id || ""), child_wtree: String(payload.child_wtree || ""), status: String(payload.status || ""), merge_commit: String(payload.merge_commit || "") };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "executing", stage_after: targetStage, transition, step: "child_result", step_workflow_task: "stage_executing", step_payload: payload, step_action: payload, state_patch: patch, events: eventList("executing", targetStage, transition, "child_result", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "child_result", stage_before: "executing", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_validating:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function findPayload(value, match, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (match(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPayload(row[key], match, depth + 1); if (match(found)) return found; } } for (const item of Object.values(row)) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; }
            function normalizePrompt(value) { return String(value || "").replace(/^loopship:\s*/i, "").trim().toLowerCase(); }
            function isTerminalChild(tasks) { return Boolean(String(tasks.parent_wtree || "").trim()) || Boolean(String(tasks.parent_task_id || "").trim()) || Boolean(String(tasks.parent_context_ref || "").trim()) || normalizePrompt(tasks.prompt).startsWith("execute child task "); }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const tasks = object(state.steps.read_tasks?.action);
            const stageAction = object(state.steps.stage_validating?.action);
            const payload = findPayload(stageAction, (row) => "checks" in object(row) && "status" in object(row));
            const status = String(payload.status || "");
            const transition = status === "passed" ? "passed" : status === "failed_child" ? "failed_child" : "failed";
            const targetStage = transition === "passed" ? "verification_pending" : transition === "failed_child" ? "validating" : isTerminalChild(tasks) ? "executing" : "task_graph_ready";
            const patch = { stage: targetStage, validation_receipt: { status, checks: array(payload.checks) } };
            const event = { event: "validation_submitted", stage: targetStage, status, check_count: array(payload.checks).length };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "validating", stage_after: targetStage, transition, step: "validation", step_workflow_task: "stage_validating", step_payload: payload, step_action: payload, state_patch: patch, events: eventList("validating", targetStage, transition, "validation", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "validation", stage_before: "validating", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_verification_pending:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function findPayload(value, match, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (match(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPayload(row[key], match, depth + 1); if (match(found)) return found; } } for (const item of Object.values(row)) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const stageAction = object(state.steps.stage_verification_pending?.action);
            const payload = findPayload(stageAction, (row) => "acceptance_trace" in object(row) || "risks" in object(row) || "status" in object(row));
            const status = String(payload.status || "");
            const transition = status === "passed" ? "passed" : "failed";
            const targetStage = transition === "passed" ? "system_update_pending" : "validating";
            const patch = { stage: targetStage, verification_receipt: { status, acceptance_trace: array(payload.acceptance_trace), risks: array(payload.risks) } };
            const event = { event: "verification_submitted", stage: targetStage, status, acceptance_count: array(payload.acceptance_trace).length, risk_count: array(payload.risks).length };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "verification_pending", stage_after: targetStage, transition, step: "verification", step_workflow_task: "stage_verification_pending", step_payload: payload, step_action: payload, state_patch: patch, events: eventList("verification_pending", targetStage, transition, "verification", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "verification", stage_before: "verification_pending", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_system_update_pending:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function array(value) { return Array.isArray(value) ? value : []; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const stageAction = object(state.steps.stage_system_update_pending?.action);
            const event = { event: "system_update_submitted", stage: "landing_ready", touched_count: array(stageAction.touched).length };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "system_update_pending", stage_after: "landing_ready", transition: "updated", step: "system_update", step_workflow_task: "stage_system_update_pending", step_payload: stageAction, step_action: stageAction, state_patch: { stage: "landing_ready" }, events: eventList("system_update_pending", "landing_ready", "updated", "system_update", event), event_payload: { event: "stage_changed", stage: "landing_ready", transition: "updated", step: "system_update", stage_before: "system_update_pending", stage_after: "landing_ready" }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_landing_ready:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function directPayload(value) { const row = object(value); const decision = object(row.decision); if (Object.prototype.hasOwnProperty.call(decision, "decision")) return decision.decision; if (Object.keys(decision).length) return decision; if (Object.prototype.hasOwnProperty.call(row, "decision")) return row.decision; return row; }
            function findPayload(value, match, depth = 0) { if (depth > 8 || !value || typeof value !== "object") return {}; if (Array.isArray(value)) { for (const item of value) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; } const row = object(value); const direct = directPayload(row); if (match(direct)) return direct; for (const key of ["output", "result", "workflow", "action", "value", "answer"]) { if (key in row) { const found = findPayload(row[key], match, depth + 1); if (match(found)) return found; } } for (const item of Object.values(row)) { const found = findPayload(item, match, depth + 1); if (match(found)) return found; } return {}; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const stageAction = object(state.steps.stage_landing_ready?.action);
            const payload = findPayload(stageAction, (row) => "status" in object(row) || "summary" in object(row));
            const status = String(payload.status || "landed");
            const targetStage = status === "blocked" ? "landing_ready" : "archived";
            const transition = status === "blocked" ? "blocked" : "landed";
            const event = { event: "landing_submitted", stage: targetStage, status: String(payload.status || transition || "") };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "landing_ready", stage_after: targetStage, transition, step: "landing", step_workflow_task: "stage_landing_ready", step_payload: payload, step_action: payload, state_patch: { stage: targetStage, landed_commit: String(payload.receipt?.landed_commit || payload.landed_commit || ""), landing_strategy: String(payload.receipt?.strategy || payload.strategy || "") }, events: eventList("landing_ready", targetStage, transition, "landing", event), event_payload: { event: "stage_changed", stage: targetStage, transition, step: "landing", stage_before: "landing_ready", stage_after: targetStage }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - stage_result_archived:
      then: persist_stage
      metadata:
        description: "Build a stage-local result from the selected stage workflow output."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      run:
        script:
          language: js
          code: |-
            function object(value) { return value && typeof value === "object" && !Array.isArray(value) ? value : {}; }
            function eventList(stageBefore, targetStage, transition, step, event) { return [{ schema_version: "1.0.0", payload: { ...event, transition, step, stage_before: stageBefore, stage_after: targetStage } }, { schema_version: "1.0.0", payload: { event: "stage_changed", stage: targetStage, transition, step, stage_before: stageBefore, stage_after: targetStage } }]; }
            function runtimeSnapshot() { const resolved = object(state.steps.resolve_stage?.action); return resolved.runtime || { tasks: object(state.steps.read_tasks?.action), manifest: null, events: state.steps.query_events?.action || [] }; }
            const stageAction = object(state.steps.stage_archived?.action);
            const event = { event: "step_submitted", stage: "archived", step: "archived" };
            return { schema_version: "loopship.stage-result/v1", flow_id: "swe", stage_before: "archived", stage_after: "archived", transition: "complete", step: "archived", step_workflow_task: "stage_archived", step_payload: stageAction, step_action: stageAction, state_patch: { stage: "archived" }, events: eventList("archived", "archived", "complete", "archived", event), event_payload: { event: "stage_changed", stage: "archived", transition: "complete", step: "archived", stage_before: "archived", stage_after: "archived" }, runtime: runtimeSnapshot() };
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action.value}"
  - persist_stage:
      metadata:
        description: "Persist the Fastflow-derived Loopship stage through workflow data."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      call: fastflow.afn.data.document.patch
      with:
        body:
          namespace: .loopship/runtime
          adapter_config:
            rootDir: .
          adapter: yaml
          document: tasks
          patch: "${(state.steps.stage_result_planning?.action || state.steps.stage_result_awaiting_user_answers?.action || state.steps.stage_result_plan_review?.action || state.steps.stage_result_task_graph_ready?.action || state.steps.stage_result_leaf_executing?.action || state.steps.stage_result_executing?.action || state.steps.stage_result_validating?.action || state.steps.stage_result_verification_pending?.action || state.steps.stage_result_system_update_pending?.action || state.steps.stage_result_landing_ready?.action || state.steps.stage_result_replanning?.action || state.steps.stage_result_archived?.action || {}).state_patch || {}}"
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
  - append_stage_event:
      metadata:
        description: "Append the Fastflow-derived stage transition event through workflow data."
        validation:
          post:
            kind: static
            ok: true
            evidence:
              generated_by: loopship.fastflow
        verification:
          assertions:
            - id: loopship_native_step_contract
              kind: behaviour
              statement: "The generated Loopship Fastflow-native step produced an action result."
              check:
                script:
                  kind: js
                  code: |
                    const ok = action !== undefined;
                    return { ok, evidence: { has_action: ok } };
      call: fastflow.afn.data.event-log.append
      with:
        body:
          namespace: .loopship/runtime
          adapter_config:
            rootDir: .
          adapter: jsonl
          log: events
          events: "${(state.steps.stage_result_planning?.action || state.steps.stage_result_awaiting_user_answers?.action || state.steps.stage_result_plan_review?.action || state.steps.stage_result_task_graph_ready?.action || state.steps.stage_result_leaf_executing?.action || state.steps.stage_result_executing?.action || state.steps.stage_result_validating?.action || state.steps.stage_result_verification_pending?.action || state.steps.stage_result_system_update_pending?.action || state.steps.stage_result_landing_ready?.action || state.steps.stage_result_replanning?.action || state.steps.stage_result_archived?.action || {}).events || []}"
      output:
        schema:
          document:
            type: object
            additionalProperties: true
        as: "${action}"
output:
  schema:
    document:
      type: object
      additionalProperties: true
  as: "${state.steps.stage_result_planning?.action || state.steps.stage_result_awaiting_user_answers?.action || state.steps.stage_result_plan_review?.action || state.steps.stage_result_task_graph_ready?.action || state.steps.stage_result_leaf_executing?.action || state.steps.stage_result_executing?.action || state.steps.stage_result_validating?.action || state.steps.stage_result_verification_pending?.action || state.steps.stage_result_system_update_pending?.action || state.steps.stage_result_landing_ready?.action || state.steps.stage_result_replanning?.action || state.steps.stage_result_archived?.action || {}}"
